.row
  .col-md-10
    .discussions.container-fluid
      - @discussions.each do |discussion|
        = render discussion

    .row.discussion.discussion-form
      %hr
      = render partial: 'form'

  .col-md-2.side-menu
    = render 'collaborators'
