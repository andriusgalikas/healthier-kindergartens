<% content_for :page_title, "Edit Survey" %>
<% content_for :previous_path, manager_subject_path(@subject) %>

<div class="row">
    <div class="twelvecol">
        <%= render partial: 'form', locals: { url: manager_subject_survey_path } %>
    </div>
</div>

