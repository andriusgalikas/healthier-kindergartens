- content_for :page_title, t('manager.todos.labels.task_manager')
- content_for :previous_path, manager_reports_todos_root_path
.todo-dash-container
  .container
    #todos.row
      .twelvecol
        .todo-list
          %h2
            =t('manager.todos.report.search_title', query: params[:query])
          - if @todos.empty?
            %p=t('manager.todos.report.search_desc')
          - else
            %ul
              - @todos.each do |todo|
                %li
                  .menu-item
                    %span.todo-id= todo.id
                    .profile-thumb
                      = image_tag todo.icon.file.thumb, class: 'img-responsive center-block' unless todo.icon.nil?
                    %span.todo-title= truncate(todo.title, length: 20)
                  = link_to t('manager.todos.labels.view_report'), manager_reports_todo_set_date_range_path(todo), :class => 'btn btn-yellow btn-normal btn-block'
