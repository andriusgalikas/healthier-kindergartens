%h1 Listing subjects

%table
  %thead
    %tr
      %th Name
      %th
      %th
      %th

  %tbody
    - @subjects.each do |subject|
      %tr
        %td= subject.title
        %td= link_to 'Edit', edit_admin_subject_path(subject)
        %td= link_to 'Destroy', [:admin, subject], :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Subject', new_admin_subject_path
