<% content_for :title, 'Edit Survey Subject' %>
<% content_for :breadcrumb, 'Edit' %>
<% breadcrumb_add 'Surveys', admin_survey_subjects_path %>

<div class="twelvecol">
    <div class="page-header">
        <h2 class="widget widget-header">Edit Survey Subject</h2>
    </div>
    <%= render partial: 'form', locals: { url: admin_survey_subject_path } %>
</div>

